# Project Status

The live, frequently-updated version of this file is published here — read that one,
not this one:

**https://claude.ai/code/artifact/bb491872-5b2a-407a-8add-71d0c35f9253**

It's an Artifact, not a repo file, specifically so a session on either machine can
read or update the current status without a `git pull`/`push` round-trip. Whoever
(whichever machine/session) changes something material should republish that same
URL with updated content — see the Artifact tool's own instructions for how to
update an existing artifact in place rather than creating a new one.

This file exists only as a durable, git-synced pointer to that URL — in case anyone
lands on the repo directly (e.g. browsing GitHub) without already knowing the link.
`ROADMAP.md` remains the full, permanent, append-only narrative history.
